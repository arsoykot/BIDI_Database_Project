For Front-End
please install the requirements - pip install -r requirements.txt
In terminal run - python3 app.py
and open browser and copy paste the link- http://127.0.0.1:5000
It should show the front-end